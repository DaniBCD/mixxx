name: Build Installers (Windows & Linux)

on:
  push:
    branches:
      - "*"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    name: Windows x64 Installer
    runs-on: windows-2022
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup MSVC developer environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Configure CMake
        shell: pwsh
        run: |
          $env:BUILDENV_URL = "https://downloads.mixxx.org/dependencies/2.7/Windows/mixxx-deps-2.7-x64-windows-1c20f84a.zip"
          $env:BUILDENV_SHA256 = "77b75ab17f06e07b1c140e90f86aafb8658d66d8d581d488ab9b9545996fdc6e"
          cmake -B build -S .

      - name: Build Mixxx Release
        shell: pwsh
        run: |
          cmake --build build --config Release --parallel

      - name: Prepare Windows Portable Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path "dist" -Force
          # Copy Qt plugins and resources to Release folder
          Copy-Item -Path "res\*" -Destination "build\Release\" -Recurse -Force
          Copy-Item -Path "buildenv\mixxx-deps-2.7-x64-windows-1c20f84a\installed\x64-windows\Qt6\plugins\*" -Destination "build\Release\" -Recurse -Force
          
          # Compress to zip for easy portable distribution
          Compress-Archive -Path "build\Release\*" -DestinationPath "dist\mixxx-windows-x64-portable.zip" -Force

      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mixxx-windows-x64
          path: dist\mixxx-windows-x64-portable.zip

  build-linux:
    name: Linux Ubuntu x64 Package
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          ./tools/debian_buildenv.sh setup

      - name: Configure CMake
        run: |
          cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      - name: Build Mixxx Release
        run: |
          cmake --build build --parallel $(nproc)

      - name: Package with CPack
        run: |
          mkdir -p dist
          cd build
          cpack -G DEB -B ../dist || true
          cpack -G TGZ -B ../dist || true

      - name: Upload Linux Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mixxx-linux-ubuntu
          path: dist/*
